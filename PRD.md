# PRD.md｜产品需求文档

**项目名称**：随手笔记（NoteLab）
**一句话定位**：一个自己用的极简笔记网页 —— 打开就能写，用 #标签 归类，能搜、能翻。
**版本**：v1.0（Day 4 定稿）
**作者**：QAQ123a
**日期**：2026-09-19
**上游文档**：`research.md`（Day 3 需求研究）

---

## 1. 背景与目标

### 1.1 背景

我的笔记散落在微信收藏、备忘录、桌面 txt 文件里，记的时候快，用的时候一个都想不起来。研究过 flomo、Obsidian、Notion 之后确认：真正决定我能不能坚持用的，不是功能多少，而是**每次记录前需要做的决定有多少**。

### 1.2 本期目标（可衡量的）

> **我自己连续使用 7 天，每天至少记 1 条，并且能在 15 秒内找到 3 天前记的一条内容。**

- 如果 7 天后我还在用 → 本期成功
- 如果 7 天后我不用了 → 功能做得再多也算失败，要回头改设计

### 1.3 成功标准（本期只认这三条）

1. 从"想记"到"记完"不超过 5 秒
2. 找一条旧笔记不超过 15 秒
3. 我不需要额外做任何"整理"动作

---

## 2. 用户与使用场景

### 2.1 用户

| 项 | 说明 |
|---|---|
| 用户数量 | 1 个（就我自己） |
| 使用设备 | 仅桌面浏览器（Chrome / Edge） |
| 技术背景 | 零基础，会用电脑，不会写代码 |
| 明确不考虑 | 团队协作、分享、多人权限 |

### 2.2 核心场景

**场景 A：随手记（最高频）**
我在看视频/上课时突然想到一件事 → 切到页面 → 直接打字 → Ctrl+Enter → 继续干我的事。全程不选文件夹、不起标题。

**场景 B：找回来**
三天前记的某句话想找出来 → 输入记得的几个字 → 立刻看到 → 复制走。

**场景 C：翻旧账**
不知道要找什么，就想看看以前记了什么 → 点「翻一翻」→ 随机出一条旧的 → 常常能想起点东西。

---

## 3. 功能清单（本期做）

| 编号 | 功能 | 优先级 | 一句话说明 |
|---|---|---|---|
| F1 | 写笔记 | P0 | 打开就能写，保存后立刻出现在列表最上方 |
| F2 | 标签归类 | P0 | 正文里打 `#标签`，自动归类，不做文件夹 |
| F3 | 关键词搜索 | P0 | 输入几个字，实时筛出相关笔记 |
| F4 | 随机回顾 | P1 | 点一下随机翻出一条旧笔记 |
| F5 | 本地保存 | P0 | 关掉浏览器再打开，笔记还在 |

> P0 = 没有它这个产品不成立；P1 = 有它体验完整，可以晚一天做。

---

## 4. 功能详述与验收标准

> 验收标准全部写成「**给定什么情况 → 我做什么 → 应该看到什么**」，Day 7 开发完逐条对照打勾。

### F1 写笔记

**用户故事**：作为一个懒得整理的人，我希望打开页面就能直接打字，以便在灵感跑掉之前把它记下来。

**功能描述**：页面顶部固定一个输入框。输入内容后按 `Ctrl + Enter` 或点「保存」按钮，笔记保存并显示在下方列表最上方，输入框自动清空并保持聚焦。

| # | 验收标准 | 通过 |
|---|---|---|
| 1.1 | 打开页面，输入框自动获得光标，不需要点任何地方就能直接打字 | ☐ |
| 1.2 | 输入内容后按 Ctrl+Enter，笔记出现在列表**最上方**，且输入框被清空 | ☐ |
| 1.3 | 内容为空（或只有空格）时保存，不生成笔记，并在输入框下方提示「先写点什么再保存」 | ☐ |
| 1.4 | 刷新页面后，刚保存的笔记仍然在列表里，顺序不变 | ☐ |
| 1.5 | 单条笔记最多 5000 字，到达上限时输入被阻止并提示 | ☐ |

### F2 标签归类

**用户故事**：作为一个不做整理的人，我希望在正文里直接打 `#标签` 就完成归类，以便省掉"这条该放哪个文件夹"的思考。

**功能描述**：笔记正文里以 `#` 开头的词自动识别为标签。页面侧边显示标签列表，点击某个标签只显示带该标签的笔记。

| # | 验收标准 | 通过 |
|---|---|---|
| 2.1 | 正文里写 `#考研`，保存后这条笔记的标签区显示「考研」 | ☐ |
| 2.2 | 标签列表按**使用次数从多到少**排序，次数相同的按名称排序 | ☐ |
| 2.3 | 点击标签「考研」，列表只显示带该标签的笔记；再次点击取消筛选 | ☐ |
| 2.4 | 一条笔记支持 0~5 个标签；超过 5 个时只保留前 5 个并提示 | ☐ |
| 2.5 | 没有任何标签的笔记，归入列表中的「无标签」分组，**不会消失** | ☐ |
| 2.6 | 标签内的文字不做任何大小写或繁简转换，原样显示 | ☐ |
| 2.7 | 标签**区分大小写**：`#Study` 和 `#study` 算两个标签（实现简单、行为可预期，不会出现"为什么它合并了"的困惑） | ☐ |

### F3 关键词搜索

**用户故事**：作为一个经常只记得半句话的人，我希望输入几个字就能翻出旧笔记，以便不用一条条往下翻。

**功能描述**：页面顶部搜索框，输入即筛选（不需要点搜索按钮）。

| # | 验收标准 | 通过 |
|---|---|---|
| 3.1 | 输入关键词，列表**实时**过滤，只保留正文或标签中包含该词的笔记 | ☐ |
| 3.2 | 搜索不区分英文大小写；中文按连续字符匹配 | ☐ |
| 3.3 | 命中的关键词在结果中高亮显示 | ☐ |
| 3.4 | 没有结果时显示「没有找到相关笔记」，而不是一片空白 | ☐ |
| 3.5 | 清空搜索框，列表恢复显示全部笔记 | ☐ |

### F4 随机回顾

**用户故事**：作为一个记完就忘的人，我希望偶尔被推送一条旧笔记，以便想起以前想过什么。

**功能描述**：页面放一个「翻一翻」按钮，点击后随机取一条已有笔记，突出显示。

| # | 验收标准 | 通过 |
|---|---|---|
| 4.1 | 点击「翻一翻」，突出显示一条已有笔记 | ☐ |
| 4.2 | 笔记总数 ≥ 2 时，连续点击不会连续两次抽出同一条 | ☐ |
| 4.3 | 笔记少于 3 条时，不抽，提示「再多记几条，翻起来才有意思」 | ☐ |

### F5 本地保存

**用户故事**：作为唯一用户，我希望数据存在自己电脑上，以便不用担心账号和隐私。

| # | 验收标准 | 通过 |
|---|---|---|
| 5.1 | 关闭浏览器再打开该页面，所有笔记仍在 | ☐ |
| 5.2 | 页面**不要求登录**，没有任何账号、密码、注册流程 | ☐ |
| 5.3 | 断网状态下打开页面，功能完全正常 | ☐ |
| 5.4 | 浏览器不允许本地存储时（如隐私模式），保存后必须提示「本次记录可能不会被保留」，而不是静默失败 | ☐ |

---

## 5. 非功能需求（刻意写得很克制）

| 项 | 要求 |
|---|---|
| 浏览器 | 只保证 Chrome / Edge 桌面版最新版正常 |
| 加载速度 | 从打开到可以打字 < 1 秒 |
| 数据存放 | 浏览器本地存储（localStorage），**不上传任何服务器** |
| 内容格式 | 只支持纯文本，不支持图片、附件、表格、富文本 |
| 容量假设 | 1000 条以内的笔记，页面不卡 |
| 移动端 | 手机上打开"能看"，但不做适配，不做 App |
| 兼容性 | 不考虑 IE、不考虑旧版浏览器 |

---

## 6. 页面结构（粗粒度，不做视觉设计）

单页面，从上到下：

```
┌──────────────────────────────────────┐
│  输入框（顶部固定，自动聚焦）  [保存]  │
├──────────────────────────────────────┤
│  搜索框                              │
├───────────┬──────────────────────────┤
│  标签列表  │  笔记列表（新的在上面）    │
│  #考研(5)  │  ┌────────────────────┐  │
│  #想法(3)  │  │ 笔记内容   #标签    │  │
│  无标签(1) │  └────────────────────┘  │
├───────────┴──────────────────────────┤
│  [翻一翻]                            │
└──────────────────────────────────────┘
```

不做：多页面跳转、路由、登录页、设置页。

---

## 7. 本期不做（含今天砍掉的功能）

> 判断标准：**没有它我照样能用 → 一律不做。**

### 7.1 今天砍掉的功能（重点说明理由）

| 砍掉的功能 | 为什么砍 |
|---|---|
| **编辑已有笔记** | MVP 阶段我只验证"记下来、找得回"。加编辑要引入编辑态、保存按钮、取消操作、光标位置四件事，页面复杂度翻倍。记错了就直接再记一条修正，成本更低 |
| **删除笔记** | 有删除就必须有"确认弹窗"和"误删恢复"，否则等于给自己挖坑。本期不删，宁可多留 |
| **置顶 / 收藏 / 星标** | 这是"整理"动作，和研究结论冲突：多一个要做的决定，就多一次不记录的理由 |
| **笔记排序方式切换** | 固定按时间倒序就够了，多一种排序就多一个要想的选择 |

### 7.2 沿用 research.md 的「本期不做」

不做账号登录、不做多用户与分享、不做云同步与跨设备、不做手机 App、不做富文本、不做多层文件夹、不做 AI 总结与问答、不做图片附件、不做导入其他笔记软件、不做插件系统、不做提醒与日历待办、不做导出功能。

> 完整理由见 `research.md` 第六节。
> 注意：导出功能虽然本期不做，但它是「下期候选」的第一位（见第 9 节的风险应对）。**本期不做的意思是不在这份文档的验收范围里，不是永远不会做。**

---

## 8. 里程碑与验收方式

| 阶段 | 内容 | 判定 |
|---|---|---|
| Day 4（今天） | PRD 定稿 | 本文档完成 + AI 自检通过 |
| Day 5 | 技术选型与任务拆解 | 能说清用什么做、分几步 |
| Day 7 | MVP 可跑 | F1~F5 的验收标准**逐条打勾**，全绿才算完成 |
| Day 7 之后 | 持续使用观察 | 连续 7 天我自己每天在用 |

**验收方式**：打开本文档第 4 节，一条一条照着操作，通过打勾。任何一条不通过 → 不许进入下一阶段，先修。

---

## 9. 风险与待确认

| 风险 | 影响 | 应对 |
|---|---|---|
| localStorage 会被浏览器清理（清缓存/隐私模式） | 笔记全丢 | 明确定位为"临时记录工具"；Day 7 之后优先补"导出为 Markdown 文件"，导出后正好用 Git 管理 |
| 换电脑就看不到笔记 | 数据不跟人走 | 本期接受；不引入云同步（成本远高于收益） |
| 功能越做越多 | 重蹈 Notion 覆辙 | 任何新想法先写进"下期候选"，本期文档冻结 |
| 我可能自己也不用 | 项目白做 | 用第 1.2 节的"连续 7 天使用"作为唯一成功判据 |

**待确认**：无（本期范围已冻结）。

---

## 附：AI 自检记录（Day 4 板块③）

请 AI 扮演挑刺的评审，按两条尺子检查本文档：**① 开发的人能不能照着直接做？② 验收标准能不能一条条打勾？**

### 第一轮（针对初稿）

| 级别 | 问题 | 处理 |
|---|---|---|
| ~~必须修改~~ | F2 原写"一条笔记最多 3 个标签"——数字是拍脑袋定的，没有依据，无法判断对错 | 已改为 0~5 个，并补上超出时的行为（保留前 5 个 + 提示） |
| ~~必须修改~~ | F1 验收 4 要求"刷新后还在"，但全文没说数据存在哪，这条验收没法执行 | 已在第 5 节明确 localStorage，并把"本地保存"单独立为 F5 |
| 建议修改 | 搜索没说大小写和中文怎么处理，开发时会各写一套 | 已补验收 3.2 |
| 建议修改 | F4「随机」没有定义"随机"到什么程度，无法验收 | 已补 4.2 连续不重复、4.3 数量下限 |
| 建议修改 | 里程碑只有终点，没有中途检查点 | 已改为 Day 5 / Day 7 两级 |
| 建议修改 | 缺少"如果我最后不用了怎么办"的判据 | 已补 1.2 与第 9 节的对应风险 |

### 第二轮（针对修改后的版本，复检）

| 级别 | 问题 | 处理 |
|---|---|---|
| ~~必须修改~~ | 标签没定义大小写：`#Study` 和 `#study` 到底算一个还是两个？会影响标签统计，也会让开发随心写 | 已补验收 2.7，明确区分大小写 |
| ~~必须修改~~ | 浏览器禁用本地存储时（隐私模式）会发生什么？没写 → 可能导致笔记"看起来存了其实没存" | 已补验收 5.4，要求明确提示而不是静默失败 |
| 建议修改 | 第 7 节写"不做导出"，第 9 节又写"优先补导出"，读者会以为是矛盾 | 已在第 7 节补充说明"本期不做 ≠ 永远不做"，并引入「下期候选」概念 |

### 结论

- 「必须修改」级问题：**0 项**（共出现过 4 项，全部已在自检中当场修掉）
- 「建议修改」级问题：7 项，已全部修掉
- 最终判定：**本文档通过自检，可进入 Day 5 技术选型**

> 自检后本文档即为冻结版：Day 5 只做技术选型，不再改需求；新想法一律记录到「下期候选」，不许临时加进本期。
