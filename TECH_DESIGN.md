# TECH_DESIGN.md｜技术方案设计

**项目**：随手笔记（NoteLab）
**版本**：v1.0（Day 5）
**依据**：`PRD.md` v1.0（Day 4 冻结版）—— 本文档不改需求，只回答"用什么做、数据怎么流"
**日期**：2026-09-20

---

## 0. 技术路线（一行说清）

> ## 技术路线：纯前端单页应用 —— 原生 HTML + CSS + JavaScript（不用框架、不用构建工具），数据存在浏览器 localStorage，用 Git / GitHub 管版本。
> ## 没有后端，也没有数据库。

**一句话版**：打开一个 `index.html` 文件就能用的网页，数据只躺在自己电脑的浏览器里，靠 Git 存档。

---

## 1. 先把三层分工想清楚

一般网站分三层干活，本项目只用得上第一层：

| 层 | 一般负责什么 | 本项目要不要 | 为什么 |
|---|---|---|---|
| **前端** | 用户看得见、点得到的部分：界面、输入、按钮、列表 | ✅ **全部** | 这就是本项目的全部 |
| **后端** | 在服务器上跑的逻辑：登录鉴权、订单计算、多人数据分发 | ❌ 不要 | 我只有 1 个用户，且没有任何需要保密或集中处理的逻辑 |
| **数据库** | 长期存储、多人多设备同时读写 | ❌ 不要 | 用浏览器自带的 localStorage 代替，它就在本机、天然私有 |

**判断依据**：后端和数据库存在的价值是"**多方共享 + 集中管理**"。
本项目是 1 个用户、1 台电脑、1 份数据 —— 引入后端和数据库只会额外带来：服务器费用、部署流程、账号系统、安全风险。**全都不划算。**

---

## 2. 技术路线与理由

| 环节 | 选什么 | 为什么选它 | 备选与否决原因 |
|---|---|---|---|
| 页面结构 | **HTML** | 网页的地基，浏览器原生支持，不用装任何东西 | — |
| 样式 | **CSS（手写）** | 单页只有几个区块，手写更可控 | ❌ Tailwind：要装构建工具，为了几个样式不值 |
| 逻辑 | **JavaScript（原生）** | PRD 只有 5 个功能，用数组和 localStorage 就够 | ❌ React / Vue：需要 Node 构建流程、要学框架语法；它的优势（组件复用、复杂状态管理）在这里用不上，出错时还多一层"构建报错"，对零基础是负担 |
| 数据存储 | **localStorage** | 浏览器自带、零依赖、关掉浏览器数据还在 | ❌ 数据库（SQLite/MySQL）：浏览器里跑不了，必须有后端；❌ IndexedDB：能力过剩，API 复杂得多 |
| 版本管理 | **Git + GitHub** | Day 2 已经配好；纯文本文件天然适合 Git | — |
| 开发方式 | 文本编辑器 + 浏览器刷新 | 不需要安装、打包、起服务器 | ❌ 各种脚手架：本项目不需要"工程化" |

---

## 3. 技术栈速览（这些词都是什么意思）

**技术栈 = 做这个产品要用到的一整套技术工具的清单。** 本项目的栈很小，一共就这些：

| 技术 | 是什么 | 在本项目负责什么 | 优势 | 我要忍受的劣势 |
|---|---|---|---|---|
| **HTML** | 网页的骨架语言，用标签描述"页面上有什么" | 输入框、搜索框、标签栏、列表这些"东西" | 浏览器直接认，无需安装 | 只能描述结构，不能做交互和美化 |
| **CSS** | 描述"长什么样"的样式语言 | 间距、颜色、圆角、移动端的粗略显示 | 同样零依赖 | 不同浏览器细节有差异，要小心 |
| **JavaScript** | 浏览器里的编程语言 | 解析 `#标签`、读写 localStorage、渲染列表、搜索、随机 | 一行代码就能操作页面，无需后端 | 逻辑写乱了很难查，要拆成小函数 |
| **localStorage** | 浏览器自带的小仓库，按"键 → 值"存字符串 | 存全部笔记，是唯一的持久化位置 | 零配置、离线可用、隐私天然 | 容量约 5MB；**清缓存会清掉**；只能存字符串（要 JSON 转换） |
| **Git / GitHub** | 版本管理工具 + 云端仓库 | 给代码存档、留改版历史、交作业 | 免费、能回退、能给别人看 | 需要理解"提交/推送"这套动作 |
| **浏览器（Chrome / Edge）** | 运行网页的程序 | 既是开发环境，也是运行环境 | 自带开发者工具 F12 可查错 | 只保证这两个，其他浏览器不承诺 |

> 对比一下：如果是"多人协作的网盘 App"，技术栈里就会多出后端框架、数据库、服务器、部署平台 —— **技术栈是被需求决定的，不是越高级越好。**

---

## 4. 文件结构（Day 7 开发完的样子）

```
vibe-coding-lab/
├── AGENTS.md         ← Day 1  项目规则（给 AI 看的）
├── research.md       ← Day 3  需求研究
├── PRD.md            ← Day 4  需求文档（已冻结）
├── TECH_DESIGN.md    ← Day 5  本文档
├── index.html        ← Day 2 占位页 → Day 7 重写为正式页面
├── styles.css        ← Day 7  新增，样式
├── app.js            ← Day 7  新增，全部逻辑
└── .gitignore        ← Day 2  忽略规则（.env 不上传）
```

---

## 5. 数据流图（核心）

```
   ① 数据从哪来（唯一来源：我的键盘）
   ┌──────────────────────────────────────────────────┐
   │ 我在输入框里打的字：正文 + #标签                    │
   └────────────────────────┬─────────────────────────┘
                            │ 按 Ctrl+Enter
                            ▼
   ② 在哪处理（app.js，浏览器内存）
   ┌──────────────────────────────────────────────────┐
   │ 拆出标签 → 组装成一条笔记对象                       │
   │ { id, content, tags[], createdAt }               │
   │ 追加进内存数组 notes[]                             │
   └────────────────────────┬─────────────────────────┘
                            │ JSON.stringify 写入
                            ▼
   ③ 存在哪（浏览器本地 —— 唯一的持久化位置）
   ┌──────────────────────────────────────────────────┐
   │ localStorage                                     │
   │ key = notelab.notes.v1                           │
   └────────────────────────┬─────────────────────────┘
                            │ 每次打开页面：JSON.parse 读回内存
                            ▼
   ④ 到哪去（全部在同一个页面里消费，不离开这台电脑）
   ┌──────────────────────────────────────────────────┐
   │ 笔记列表 ← 按时间倒序渲染 notes[]                  │
   │ 搜索过滤 ← 按关键词筛 notes[]                      │
   │ 随机回顾 ← 从 notes[] 里随机取一条                 │
   └──────────────────────────────────────────────────┘

   全程不出现：网络请求、服务器、数据库、账号密码
```

### 5.1 一句话总结

> **数据只从我的键盘来，只到我这台电脑的浏览器里去 —— 中间不经过任何服务器。**

### 5.2 两种走法（对应两个场景）

**写入流（记录）**：输入框 → app.js 拆标签组装对象 → 追加进 `notes[]` → `JSON.stringify` 存进 localStorage → 重新渲染列表。

**读取流（搜 / 翻）**：打开页面 → `JSON.parse` 从 localStorage 读回 `notes[]` → 在内存里 filter（搜索）或随机取一条（回顾）→ 渲染到页面。**搜索不会去读写存储，只操作内存**，所以是实时的。

---

## 6. 数据模型（就一个数组）

```js
// localStorage 里的结构
{
  "notelab.notes.v1": "[ {...}, {...} ]"   // 存的是字符串，读出来再 JSON.parse
}

// 每条笔记长这样
{
  "id": "n_1726905600000",     // 用时间戳生成，不重复即可
  "content": "今天想到一个点子 #想法",
  "tags": ["想法"],            // 从 content 里拆出来，最多 5 个，区分大小写
  "createdAt": "2026-09-20T14:00:00.000Z"
}
```

**为什么不做索引、不分表**：1000 条以内，全部读进内存用 `filter` 筛，速度完全够。等真的卡了再优化（PRD 已把"1000 条内不卡"写进非功能需求）。

---

## 7. 必须遵守的硬约束

1. **不加任何第三方依赖**，不用 npm、不用构建工具 —— 双击 `index.html` 就能打开使用
2. **不发任何网络请求** —— 断网状态下必须 100% 可用（PRD 验收 5.3）
3. **不引 CDN 上的字体和图标库** —— 离线和弱网也要正常显示
4. **存储读写集中在一个地方**（`app.js` 里的两个函数 `loadNotes()` / `saveNotes()`），其他代码不许直接碰 localStorage —— 这样以后换存储方案只改这两处
5. 单个文件超过 500 行就拆开

---

## 8. 加练：方案一旦变化，哪些文件会受影响？

| 如果发生这种变化 | 受影响的文件 | 影响程度 | 具体要改什么 |
|---|---|---|---|
| 改一条验收标准的措辞（比如把"最多 5 个标签"改成 3 个） | `PRD.md` | 小 | 只改数字 + 对应的验收项 |
| 新增一个小功能（比如"导出为 Markdown"） | `PRD.md`、`TECH_DESIGN.md`、`app.js`、可能 `index.html` | 中 | 需求加一节；数据模型不变；加一个导出函数和一个按钮 |
| 换存储方式（localStorage → IndexedDB） | `TECH_DESIGN.md`、`app.js` | 小（因为约束 4） | 只改 `loadNotes()` / `saveNotes()` 两个函数 |
| 加"多设备同步" | `research.md`、`PRD.md`、`TECH_DESIGN.md` + 新增后端与部署文件 | **大** | 研究结论要重写；PRD 的"不做清单"被推翻；TECH_DESIGN 从"纯前端"变成"前端 + 后端 + 数据库"；还要买服务器 |
| 换前端框架（React） | `TECH_DESIGN.md` + 全部前端文件重写 + 引入构建流程 | **大** | 等于重做一遍，收益只有"代码更工程化" |
| 复刻一份给别人用 | `PRD.md`、`TECH_DESIGN.md` | 中 | 用户从 1 个变成 N 个 → 立刻需要账号、后端、数据库（即上一行那个大变化） |

**这张表教我的事**：改需求是"小"的，改技术路线是"大"的。所以**需求阶段多想几天，比开发阶段返工便宜得多**；这也是为什么今天不许回头大改 PRD，却要在动手前把存储读写圈在一个地方。

---

## 9. 风险与待确认

| 风险 / 待确认 | 影响 | 应对 |
|---|---|---|
| localStorage 被清缓存清掉 | 笔记全丢 | 已列入 PRD 风险；Day 7 之后第一件事做"导出为 Markdown"，导出后用 Git 存档 |
| 输入框里 Enter 该换行还是保存？ | 影响用起来顺不顺手 | **待确认**：约定 Enter 换行、Ctrl+Enter 保存（PRD 验收 1.2），Day 7 实际用一次再定 |
| 中文标签和英文标签混排的显示 | 只影响观感 | 不做特殊处理，原样显示（PRD 验收 2.6） |
| 笔记很多时列表变慢 | 页面卡顿 | 以 1000 条为界，超过再做分页或虚拟滚动 |

---

## 10. 一页总结

- **技术路线**：纯前端单页（原生 HTML/CSS/JS）+ localStorage + Git，没有后端、没有数据库
- **数据从哪来**：只从我的键盘来（输入框里的正文和 `#标签`）
- **到哪去**：只到这台电脑的浏览器里（localStorage → 渲染成列表 / 搜索 / 随机回顾）
- **为什么这么简单**：因为需求本身简单 —— 1 个用户、1 台设备、不共享、不联网
