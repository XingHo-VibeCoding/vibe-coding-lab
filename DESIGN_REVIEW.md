# DESIGN_REVIEW.md｜前端设计审查（Day 9）

> 这份文件记录的是一次**有依据的**前端审查：先定规则，再按规则找问题，最后按问题改代码。
> 不是"我觉得哪里不好看"——每条问题都要能指出违反了哪条规则、差多少。

---

## 一、先定规则：六条

审查必须有尺子，否则就变成主观喜好之争。这六条覆盖四类东西：

| 编号 | 规则 | 门槛 | 属于哪一类 |
|---|---|---|---|
| **R1** | 对比度：普通文字 ≥ **4.5:1**；界面控件边界 ≥ **3:1** | WCAG 2.1 AA | 有客观标准 |
| **R2** | 文字只分三级（主 / 次 / 弱），但**三级都要过 R1** | — | 一致 |
| **R3** | 间距只用 4px 网格，不写 7px / 9px / 14px 这类随手值 | — | 一致 |
| **R4** | 同一栏里的文字，左边缘要落在同一条竖线上 | — | 一致 |
| **R5** | 键盘 Tab 能到的元素，必须**看得见焦点** | — | 边界情况 |
| **R6** | 任何长度的内容都不能**撑破**它所在的卡片 | — | 边界情况 |

为什么要分这四类：**"像不像一个正经产品"，百分之八十靠"一致"，百分之二十靠"边界不塌"。**
不一致是"练习稿"最明显的特征（每个地方各写各的数值）；边界塌掉是"没测过"的特征。

### 对比度怎么算（不能靠眼睛）

肉眼分辨不出 2.9:1 和 4.5:1 —— 前者对视力弱的人就是"看不清"，后者才是"看得清"。
本次审查用 WCAG 公式算实际比值（脚本见本文件第七节）：

```
相对亮度 L = 0.2126*R + 0.7152*G + 0.0722*B     （R/G/B 先做 sRGB 反伽马）
对比度    = (L_亮 + 0.05) / (L_暗 + 0.05)
```

---

## 二、审查结果

按规则扫了一遍 `styles.css` + `index.html` + `app.js`，**发现 6 类问题，修复 6 类**。

| # | 问题 | 违反 | 证据（实测） | 修复 |
|---|---|---|---|---|
| **1** | 最弱一档文字发灰，看不清 | R1 R2 | `--text-faint #9a9893` 只有 **2.88:1**（卡片上）/ **2.69:1**（页面底色上），门槛 4.5:1 | 改 `#706e68` → **5.10:1 / 4.75:1**；同时把 `--text-weak` 从 `#6b6a66` 压到 `#5a5852`（7.11:1），保住三级层次 |
| **2** | 搜索框和「翻一翻」按钮的边框几乎看不见，看起来像一段文字而不是控件 | R1 | `--border #e5e3dc` 只有 **1.20:1 / 1.28:1**，门槛 3:1 | 新增 `--border-strong #8f8c85`（**3.13:1 / 3.36:1**），只用在交互控件上；卡片描边保持淡色（容器不属于 3:1 要求），另加一层极淡阴影让它浮起来 |
| **3** | 侧栏标题和下面的标签文字**左边缘差 9px**，整栏看着松 | R4 | 标题从 12px 开始；标签文字从 12+1(描边)+8(内边距)=**21px** 开始 | `.sidebar-title` 加 `padding-left: 9px`，两边都对齐到 21px |
| **4** | 间距没有体系：全文件散着 18/24/12/20/60/14/10/9/7/5/4/6/30 各种值 | R3 | 逐一列出后无基准可循 | 收敛到 4px 网格 `--space-1…8`；顺带把**控件高度统一**：搜索框和按钮都是 ≈39px（原来是 36.8 vs 38.8，并排时高矮不一） |
| **5** | 从「加载中」切到「有数据」时**整页跳一下** | R3 R4 | 骨架卡内边距 14px vs 真实卡片 12px 14px；骨架只有 2 条灰线，真实卡片是"正文两行 + 元信息一行" → 每张差约 12px，三张差 36px | 内边距统一成 `--space-3 / --space-4`；骨架卡补第 3 条灰线；加 `min-height: 74px`（= 单行笔记卡片的高度 12+22.4+8+19.2+12） |
| **6** | 边界情况会塌（4 小处） | R5 R6 | 见下 | 见下 |

### 问题 6 的 4 小处

| 小处 | 违反 | 症状 | 修复 |
|---|---|---|---|
| 超长标签 | R6 | `#` 后跟一串长字符时，标签胶囊比卡片还宽，横向溢出 | `.note-tag` 加 `max-width: 100%` + `overflow-wrap: anywhere` |
| 侧栏长标签名 | R6 | 长标签名把右边的计数挤出去 | `app.js` 里把标签名包一层 `.tag-label`，样式用省略号收住（`overflow:hidden` + `text-overflow:ellipsis`） |
| 窄屏搜索框 | R6 | 输入框默认不肯收缩到内容宽度以下，会把「翻一翻」挤出屏幕 | `.toolbar input` 加 `min-width: 0` |
| 键盘焦点 | R5 | 用 Tab 键走一圈，完全看不出焦点在哪（默认焦点圈被 `outline: none` 抹掉） | 全局加 `:focus-visible { outline: 2px solid var(--blue); outline-offset: 2px }` |

### 顺带修掉的两处小问题

- **选中标签的计数**：浅青底上的 `--text-faint` 是 **4.49:1**，差 0.01 就是不达标 → 选中态计数改用 `--teal`（**5.46:1**）。
- **骨架灰条**：`#eeede8` 在卡片上只有 **1.17:1**，几乎看不见，"正在加载"反而看不出来 → 加到 `#e4e1d9`（**1.31:1**）。
- **圆角**：原来混用 10 / 8 / 6 / 999 四种值 → 收敛到三种（`--radius` 10px / `--radius-sm` 8px / `--radius-pill` 999px）。

### 修复前后总账

```
修复前不达标：6 项
修复后不达标：0 项
```

---

## 三、改动清单

| 文件 | 归属 | 改了什么 |
|---|---|---|
| `styles.css` | Day 7 新建 → **Day 9 修改** | 调色板（文字三级 + `--border-strong`）、间距令牌（4px 网格）、对齐、骨架卡与真实卡片等高、`:focus-visible`、`.tag-label` 省略号、`.note-tag` 防溢出、圆角与阴影 |
| `app.js` | Day 7 新建 → **Day 9 修改** | ① 三处标签名包 `.tag-label` ② 骨架卡补第 3 条灰线 ③ 文件头进度注释加 Day 9 一行 |
| `index.html` | Day 7 重写 → 本日**未改** | 结构没问题，改的全是样式与渲染细节（符合「今日不做：大改版面结构」） |
| `DESIGN_REVIEW.md` | **Day 9 新建** | 本文件 |

---

## 四、余力加练：把一条视觉规则写成可复用组件约束

规则如果只写在文档里，下次加新元素时就会忘。所以把 **R2 / R3 / R6** 直接写成
**笔记卡片组件的约束**，放在 `styles.css` 里 `.note-item` 上方，跟着代码走：

```css
/* 组件约束：笔记卡片 .note-item（Day 9 加练）
   R2 卡片里的次级文字（时间戳、标签计数）只能用 --text-faint / --text-weak / --text
      这三档，不许再写死一个浅灰。Day 9 之前就是写死 #9a9893，只有 2.88:1。
   R3 卡片内边距只能用 --space-3 / --space-4。改密度就整块改，
      否则"加载中的骨架卡"和"真实卡片"会错位，切换时整页跳一下。
   R6 卡片里任何一段内容都要能断行，长标签、长链接都不许撑破卡片。 */
```

**"规则"和"约束"的区别**：规则是给人看的（会忘），约束是给代码看的（挡在改的地方）。
一条约束能替掉十次口头提醒。

---

## 五、今日要掌握：哪一处影响最大

> 今天改的几处里，哪一处对「像不像一个正经产品」影响最大？

**答：问题 1 —— 最弱那档文字的对比度（`#9a9893` → `#706e68`）。**

三条理由：

1. **覆盖面最大。** 这一档颜色出现在页面上几乎所有"细节文字"上：标题下的副标题、字数计数、
   每条笔记的时间戳、标签计数、侧栏小标题、输入框里的占位提示、空状态文案、加载提示。
   改一个变量，整页同时变。其他几处都是局部的。
2. **它落在最伤人的区间。** 2.88:1 不是"看不见"，是"能看见但发虚"。
   这个区间最麻烦：用户不会说"对比度不够"，只会觉得"这东西不太行"。
   从 2.88:1 到 5.10:1，同一句话从"灰蒙蒙"变成"清楚"。
3. **它是"专业感"最便宜的来源。** 专业产品的次级文字从不发虚，因为它们的色板是**算过**的，
   不是挑出来的。改一个变量就能拿到，性价比最高。

**紧随其后的是问题 2（控件边界）。** 它决定"用户看不看得出哪里能点"——那是可用性，不只是好看。
**问题 3、4（对齐、间距）属于"看得见但说不出"的基本功**：改完你会觉得页面"顺了"，
但说不出改了哪——这正是它们容易被长期忽略的原因。

---

## 六、怎么自己复核

两个网址放同一份假数据，只有样式不同，方便在同一位置对比：

```
修复前（对照版）  http://localhost:8124/?demo=1&state=success
修复后（正式版）  http://localhost:8123/?demo=1&state=success
```

- 对照版来自 `C:\D\Projects\vibe-coding-lab-before`（仓库**外面**的一份副本，不进 Git）
- 两个网址都带 `?demo=1&state=success`，所以显示的是同样的 10 条假笔记，对比才有意义
- 重点看同一位置：卡片里时间戳那行的颜色深浅、搜索框和「翻一翻」的边框、侧栏标题与标签的左边缘、卡片之间的间距

---

## 七、记录

- **2026-09-24（Day 9）**：按 R1–R6 六条规则做第一次前端审查，发现并修复 6 类问题（其中 2 类文字对比、
  3 处控件边界、1 处对齐、1 处间距体系、1 处布局跳动、4 处边界塌陷）。
  对比度用 WCAG 公式脚本实测，不靠肉眼：修复前 6 项不达标 → 修复后 0 项。
  加练：把 R2/R3/R6 写成 `.note-item` 组件的约束注释，跟代码放一起。
