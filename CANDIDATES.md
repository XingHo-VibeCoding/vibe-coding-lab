# CANDIDATES.md｜下期候选清单

> **规则（AGENTS.md 第六节第 2 条）**：本期做什么，以 `PRD.md` 冻结版为准。
> 任何新想法先写到这份文件里，等我说了"进入下一期"，才动手做。
> 这份文件的作用是：**让想法有地方去，但不让它插队。**

---

## 一、总览

| 编号 | 需求 | 提出时间 | 类型 | 改动代价 | 建议顺位 |
|---|---|---|---|---|---|
| C1 | 导出为 Markdown 文件 | Day 4 / Day 7 | 数据安全 | 小 | **1** |
| C2 | 删除笔记（带撤销） | Day 8 | 清理 | 中 | **2** |
| C3 | 手动拖动排序笔记 | Day 8 | 整理 | 大 | 4 |
| C3b | 笔记置顶（C3 的轻量替代） | 本次评估提出 | 整理 | 小 | 3 |
| C4 | 手动调整标签顺序 | Day 8 | 整理 | 小~中 | 5 |
| C5 | 编辑已有笔记 | Day 4 | 修正 | 中 | 6 |
| C6 | 云同步 / 跨设备 | Day 4 | 扩展 | 很大 | 最后 |

---

## 二、逐条评估

### C2 删除笔记（带撤销）—— 今天提的

**要什么**：能把没用的笔记删掉，但删错了要能救回来。

**当初为什么没做**：PRD 第 7.1 节明确砍掉了删除，原话是"有删除就必须有'确认弹窗'和'误删恢复'，否则等于给自己挖坑"。今天确认了做法：**用"撤销"代替"确认弹窗"** —— 不打断操作，也不怕误删。

**改动代价**：中。涉及 `app.js`（列表卡片加按钮、撤销逻辑）、`index.html`（撤销提示条）、`styles.css`、`PRD.md`（要新增验收标准）。
**数据层不用改结构** —— 删除就是数组里少一项，不涉及字段变化，老数据不会出问题。

**必须先定的三个点（否则做出来会出怪事）**：
1. **撤销窗口多长**：建议 5 秒。窗口内笔记从列表消失，但仍留在内存里没真正删除。
2. **窗口内刷新页面怎么办**：建议"刷新 = 视为确认删除"（简单可预期），这条必须写进验收标准，不然用户会以为"我还没点确认怎么就没了"。
3. **和"翻一翻"的关系**：删掉的笔记不能再被随机翻出来；如果它正好是上一次翻到的那条，`lastRandomId` 要清掉。

**验收标准草案**（下期直接搬进 PRD 第 4 节）：
- 每条笔记上有一个删除按钮
- 点击后笔记立即消失，底部出现「已删除 · 撤销」，5 秒后自动收起
- 5 秒内点「撤销」→ 笔记回到原来的位置，标签、时间戳都不变
- 5 秒后或期间刷新页面 → 真删除，找不回
- 删除一条笔记不影响其他笔记的顺序和标签统计

> **为什么它是第 2 顺位**：这不算"整理"，算"清理" —— 没用的东西留着才是负担，所以它和 research.md 的设计原则不冲突。

---

### C3 手动拖动排序笔记 —— 今天提的

**要什么**：用鼠标把笔记拖到想要的位置。

**⚠️ 这条和现有设计是根本冲突的，必须讲清楚**：

1. **和"按时间倒序"冲突**：一旦能手动排序，时间就不再是排序依据了 —— 也就是说，**"最新记的排最上面"这个默认行为要作废**。新笔记该插到哪？顶部还是末尾？这个问题没有正确答案，但必须选一个。
2. **要改数据模型**：每条笔记要新增一个"顺序"字段；而你已经记了真实笔记，**老数据要写迁移逻辑**（没有该字段的笔记按时间补上顺序）。
3. **筛选时要禁用拖动**：搜索或按标签筛选后，列表是"部分笔记"，此时拖动顺序没有意义（会把没显示的那些笔记的顺序搞乱）。
4. **拖拽交互本身不轻松**：HTML5 拖放或鼠标事件 + 触摸事件的兼容，移动端体验尤其差（PRD 第 5 节明确不做移动端适配）。

**和设计原则的冲突**：research.md 的结论是"少一个要先想一下的步骤，比多十个功能重要"。手动排序是典型的**整理型功能** —— 它会让每次记完笔记多冒出一个念头："这条要不要往前提一提？"

**我的建议**：先不做，观察自己到底有没有这个痛点。

---

### C3b 笔记置顶（C3 的轻量替代）—— 评估时提出

如果真实的痛点是"某几条重要的总想放在最上面"，那**不需要全量拖动排序，只要"置顶"就够**：
- 每条笔记加一个"置顶"开关，置顶的排在最上面（置顶内部仍按时间倒序），其余按时间倒序
- 数据上只多一个布尔字段，不动排序逻辑；新笔记的行为也不用纠结
- 代价：小。一次性把"位置"这个需求解掉，而且不多制造"整理"负担

**建议**：如果要做"位置"这件事，做 C3b，不做 C3。

---

### C4 手动调整标签顺序 —— 今天提的

**要什么**：侧边栏里标签的先后顺序能自己调。

**现状**：PRD 验收标准 2.2 规定"标签按使用次数从多到少排序"，次数相同按名称排。

**改动代价**：小~中。要新增一份标签顺序的存储；侧边栏要给每个标签加"上移/下移"（或拖拽）；同时**要改写 PRD 验收标准 2.2**（这条会被作废或变成"默认顺序"）。

**要先定的点**：
1. 新出现的标签排在哪里？（建议：末尾）
2. 某个标签的笔记被删光后，它在顺序表里怎么处理？（建议：保留顺序记录，等它再出现时仍在原位）
3. 「全部」和「无标签」这两个固定项要不要允许调？（建议：固定在两端，不可调）

**我的建议**：如果只是"常用的想固定在前面"，同样可以用**标签置顶**解决，比全量手动排序更省事。

---

### C1 导出为 Markdown —— 仍在第一位

理由不变：现在笔记只存在浏览器里，**清一次缓存就全没了**。导出功能做完，才能把笔记纳入 Git 管理。代价小，收益最大，建议它继续排第一。

### C5 编辑已有笔记 / C6 云同步

维持 PRD 第 7 节的判断，暂不展开（C6 代价最大，会推翻"纯前端"这条技术路线）。

---

## 三、要开工的话，按这个顺序来

1. 你明确说「**进入下一期**」，并告诉我做哪几个编号（没这句话，我不动手 —— AGENTS.md 第六节第 2 条）
2. **先改 `PRD.md` 的验收标准，再改代码**，顺序不能反（否则代码就成了"无据可依"）
3. 涉及数据模型变化的（C3 / C4）先写迁移方案，避免你已经记的笔记出问题
4. 做完按老规矩：本地验证 → 提交 → 推送 → 你截图验收

---

## 四、记录

- **2026-09-24（Day 8）**：用户提出三条需求 —— ① 能删除没用的笔记 ② 笔记位置可调 ③ 标签位置可调。
  已确认：① 采用"撤销式删除" ② 指的是"手动拖动排序" ③ 指的是"侧边栏内标签排序"。
  结论：全部记入本清单，**本期（第 1–2 周）不动手**。
