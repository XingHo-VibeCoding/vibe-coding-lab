# 随手笔记 NoteLab

一个自己用的极简笔记网页：**打开就能写，用 `#标签` 归类，能搜、能翻。**
不登录、不上云、不联网 —— 数据只存在自己这台电脑的浏览器里。

> Day 7 完成 MVP。技术路线见 `TECH_DESIGN.md`，功能与验收标准见 `PRD.md`。

---

## 一、怎么运行（运行说明存档）

本项目是纯静态网页，**两种打开方式，任选一种**。

### 方式 A：本地服务器（推荐，地址栏会是 localhost）

在项目文件夹里打开终端，执行：

```bash
cd C:/D/Projects/vibe-coding-lab
python -m http.server 8123 --bind 127.0.0.1
```

然后浏览器访问：

```
http://localhost:8123/
```

停止服务：在终端按 `Ctrl + C`。

> 本机 python 路径（如果 `python` 命令找不到）：
> `C:\Users\岚\.workbuddy\binaries\python\versions\3.13.12\python.exe`

### 方式 B：直接双击（最省事）

直接双击 `index.html` 用浏览器打开也能用，全部功能正常。
缺点：地址栏显示的是 `file:///...` 而不是 `localhost`。

### 浏览器要求

只保证 Chrome / Edge 桌面版最新版正常（PRD 第 5 节）。

---

## 二、怎么用（对照 PRD 的五个功能）

| 功能 | 怎么用 |
|---|---|
| **F1 写笔记** | 打开页面光标就在输入框里，直接打字；按 `Ctrl + Enter` 或点「保存」 |
| **F2 标签归类** | 正文里写 `#标签`（例如 `#想法`），保存后自动归类；点左侧标签可筛选，再点一次取消。标签区分大小写，一条最多 5 个 |
| **F3 关键词搜索** | 顶部搜索框输入即筛选，正文和标签都能搜到，命中的词会高亮 |
| **F4 随机回顾** | 点「翻一翻」，随机翻出一条旧笔记（笔记少于 3 条时会提示先多记几条） |
| **F5 本地保存** | 自动保存，关掉浏览器再打开笔记还在；不需要登录 |

---

## 三、演示模式与四种页面状态（Day 8 新增）

页面主体有四种状态，任何一种都不能少：

| 状态 | 什么时候出现 | 页面表现 |
|---|---|---|
| **加载中** | 数据还没取回来 | 三条灰色骨架卡片 + 「正在取数据…」 |
| **有数据** | 正常情况 | 笔记卡片列表（标签 + 时间） |
| **空** | 取数成功但一条都没有 | 一句明确的话，告诉用户下一步做什么 |
| **出错** | 取数失败 | 说明失败原因 + 一个「重试」按钮 |

本地预览这四种状态（网址后面加参数即可）：

```
http://localhost:8123/?demo=1&state=loading    加载中
http://localhost:8123/?demo=1&state=success    有数据
http://localhost:8123/?demo=1&state=empty      空
http://localhost:8123/?demo=1&state=error      出错
```

- `?demo=1` 表示**演示模式**：数据来自 `mock-data.js` 的 10 条假笔记，不会写入你的真实笔记
- 不加 `?demo=1` 就是正常模式，用你自己记录的真实数据
- 演示模式下保存会被拦下并提示，避免假数据混进真数据

> **为什么现在用假数据**：先把"数据怎么显示"这件事做对，再解决"数据从哪来"。
> 第 3 周接真实接口时，**只改 `mock-data.js` 这一个文件**，页面和组件都不用动。

### 小坑记录：链接别写 `./`

提示条里的「退出演示」当初写成了 `<a href="./">`，结果**用文件方式打开（双击 `index.html`）时点它会跳到文件夹列表**，而不是回到页面。

原因：`./` 的意思是"当前目录"。用服务器打开时，当前目录的默认文档是 `index.html`，看着没事；但用 `file://` 打开时，当前目录就是那个文件夹本身，浏览器只能把文件夹内容列出来。

**规则：要跳回页面就明确写 `index.html`，不要用 `./`。** 两种打开方式下都正确。

---

## 四、数据存在哪 / 怎么备份

- 存在浏览器的 localStorage 里，键名：`notelab.notes.v1`
- **风险**：清理浏览器缓存、使用隐私模式会清掉数据（PRD 第 9 节已记录）
- 目前的备份方式：导出功能还没做（已列入「下期候选」第一位）

---

## 五、项目文件结构

```
vibe-coding-lab/
├── AGENTS.md         Day 1 创建 / Day 6 追加   项目规则（给 AI 看的协作约定）
├── research.md       Day 3                     需求研究（三个同类产品对比）
├── PRD.md            Day 4                     需求文档（五个功能 + 26 条验收标准）
├── TECH_DESIGN.md    Day 5                     技术方案（纯前端路线 + 数据流图）
├── index.html        Day 2 占位页 → Day 7 重写  页面结构
├── styles.css        Day 7 新建 / Day 9 修改   样式（Day 9 按 6 条设计规则审查修复）
├── app.js            Day 7 新建 / Day 9 修改   全部逻辑（Day 8 四态与卡片组件；Day 9 标签名包 span、骨架卡补第三条线）
├── mock-data.js      Day 8 新建                假数据与"假接口"（第 3 周换真实接口）
├── README.md         Day 7 新建                本文件（运行说明）
├── CANDIDATES.md     Day 8 新建                下期候选清单（新想法的唯一去处）
├── DESIGN_REVIEW.md  Day 9 新建                设计审查：规则、问题、修复对照
├── skills/notelab-filter-check/  Day 12 新建   筛选交互自检 Skill（SKILL.md + run.js + RUNLOG.md）
└── .gitignore        Day 2                     忽略规则（.env 不上传）
```

### 数据流（一句话）

输入框 → `app.js` 拆标签组装笔记对象 → 存进 localStorage 的唯一位置（`loadNotes()` / `saveNotes()` 两个函数）
→ 打开页面时读回内存 → 渲染成列表 / 搜索 / 随机回顾。**全程不经过任何服务器。**

---

## 六、怎么改（约束，来自 TECH_DESIGN 第 7 节）

1. 不加任何第三方依赖、不用 npm、不用构建工具
2. 不发任何网络请求，断网必须完全可用
3. 不引 CDN 上的字体和图标
4. 存储读写只能写在 `loadNotes()` / `saveNotes()` 里，别处不许直接碰 localStorage
5. 单个文件超过 500 行就拆开
6. **改样式必须过对比度门槛**（Day 9 起）：普通文字 ≥ 4.5:1，按钮和输入框的边框 ≥ 3:1。
   规则、算法和自检小脚本见 `DESIGN_REVIEW.md`——加新颜色前先算，别靠眼睛估
7. **改了筛选相关代码先跑回归**（Day 12 起）：`node skills/notelab-filter-check/run.js`
   三种情况全 PASS 再提交，调用记录自动留在 `skills/notelab-filter-check/RUNLOG.md`

---

## 七、还没做的事（下期候选）

1. 导出为 Markdown 文件（防数据丢失，优先级最高）
2. 编辑已有笔记
3. 删除笔记（需要先设计误删恢复）
4. 云同步 / 跨设备

> 以上都**不在本期范围**（PRD 第 7 节「本期不做」），想做了要先说，不直接开工。
