# AGENTS.md - 项目规则文件

> 这个文件是给 AI 看的"项目说明书"。每次 AI 进入这个文件夹工作，
> 都会先读它，就像新员工入职先看公司手册一样。

## 一、我是谁（项目背景）

- 项目主人：零基础学习者，正在学习 vibe coding（用自然语言和 AI 协作写代码）
- 项目阶段：Day 1 起步，从环境搭建开始，暂无代码
- 项目用途：存放整个课程期间的练习、笔记和作品

## 二、语言规则

1. **始终用简体中文**回复和写注释
2. 解释技术概念时，**先打比方、再讲术语**，假设我不懂任何编程名词
3. 每出现一个新术语，用一句话解释它是什么

## 三、协作规则

1. **动手之前先说计划**：要新建/修改/删除任何文件，先告诉我打算怎么做
2. **小步前进**：一次只做一件事，做完确认再做下一件
3. **重要操作先确认**：删除、覆盖、推送代码等操作，必须先问我
4. **卡住就问**：遇到需求不清楚的地方，先提问，不要自己猜

## 四、代码规则

1. 所有代码都要带中文注释，解释每一块在做什么
2. 文件名用英文小写加连字符（如 `my-page.html`），避免中文文件名
3. 改完代码要说明：改了什么、为什么改、我该怎么验证
4. 报错时不要只丢解决方案，要顺便解释错误是什么意思

## 五、学习规则

1. 你的目标是**教会我**，不是替我完成——解释优先于执行
2. 每完成一个任务，用 1-2 句话总结我学到了什么
3. 如果我方向错了，直接指出来，不用客气

---

## 六、任务边界规则（Day 6 追加）

1. **当天只做当天任务，不许越界**：我说了今天做哪天的任务，就只做那天的内容。我没有说"进入下一板块"时，不要自行去做后面的任务，也不要顺手"优化""补全"我没要求的东西；想到要做的，先说给我听。
2. **范围冻结**（防功能膨胀）：本期做什么，以 `PRD.md` 冻结版为准。任何新想法不直接开工，先记进「下期候选」，等我说"进入下一期"再动。理由：这一周研究 Notion 的教训就是——功能越多，我越不记。
3. **改动前列清单**（防文件被悄悄改乱）：动手前必须列出"准备新建 / 修改 / 删除哪些文件"，等我确认后再执行；如果实际改动超出了清单范围，要立刻停下来告诉我。
4. **提交前查密钥**（防泄密）：每次 `git commit` 前必须检查暂存区里没有 `.env`、密码、密钥类文件（`git status --short` 中不应出现 `.env`），并把检查结果告诉我。理由：`.gitignore` 只能拦"没被跟踪过"的文件，靠人盯不住，必须每次主动核对。

### 本节备注

- 第一节「项目阶段」写的还是 Day 1 的状态（属历史记录）。按"只追加、不重写"的约定暂不修改，等整体规则需要整理时再一起更新。
- 本节规则全部为追加内容，Day 1 写入的 14 条规则原文未做任何改动（已用 `git diff` 核对：本次改动为新增 14 行、删除 0 行）。
